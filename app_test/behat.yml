default:
  suites:
    default:
      contexts:
        - FunctionalTest\Context\ExtensionContext:
            extensionConfig:
              logger:
                path: %behat_utils_extension.logger.path%
                level: %behat_utils_extension.logger.level%
              step_logger:
                enabled: %behat_utils_extension.step_logger.enabled%
        - FunctionalTest\Context\LoggerContext:
          - %paths.base%/%behat_utils_extension.logger.path%
        - FunctionalTest\Context\BehatEventsContext: ~
        - FunctionalTest\Context\BehatStepLoggerContext:
          - %paths.base%/%behat_utils_extension.logger.path%
  extensions:
    Yoanm\BehatUtilsExtension:
        logger:
            level: DEBUG
        step_logger: true
